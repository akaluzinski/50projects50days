const toggleButton = document.getElementById('toggle-button');
const navigation = document.getElementById('navigation');

toggleButton.addEventListener('click', () => navigation.classList.toggle('active'););
